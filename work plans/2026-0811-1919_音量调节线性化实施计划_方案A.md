# 音量调节线性化实施计划（方案A：prototype 劫持 + 补偿曲线）

> **状态**：第一阶段已完成（CP1 通过），第二阶段进行中
> **范围**：`gui/main_window.py`、`gui/volume_compensation.js`（新建）、`profile_manager.py`、`gui/settings_dialog.py`
> **时间**：2026-08-11 19:19 (UTC+8)（设计）；2026-08-17 06:50 (UTC+8)（第一阶段实施）
> **优先级**：中

---

## 1. 背景

套壳目标为 `https://music.163.com/st/webplayer`，音量完全由网页播放器内部控制。其滑块位置 → `audio.volume` 的映射为压缩曲线，导致滑块 10 以下音量过小、10 以上增长过陡，听感明显非线性。

选型结论见 `2026-0811-1914_音量调节线性化方案选型.md`：采用 **方案 A**——在页面脚本运行前劫持 `HTMLMediaElement.prototype.volume` 的存取器，写入时施加补偿曲线 `h(v)=v^k`（k<1 抬高低段），读取时做逆变换保持网页 UI 无感知。

已确认前提：该 webplayer 页面使用 `<audio>` 元素播放，prototype 级劫持可覆盖播放器动态创建的所有音频元素。

---

## 2. 技术设计

### 2.1 注入脚本（新建 `gui/volume_compensation.js`）

核心逻辑：

- 用 `Object.getOwnPropertyDescriptor(HTMLMediaElement.prototype, 'volume')` 取得原生 setter/getter；
- `Object.defineProperty` 重写：setter 写入 `Math.pow(v, k)`，getter 返回 `Math.pow(real, 1/k)`；
- k 挂在 `window.__NC_VOLUME_K` 上读取，默认 0.5，Qt 侧可在运行时通过 `runJavaScript` 修改该值实现热更新，无需重载页面；
- getter 逆变换是关键：播放器把音量持久化到 localStorage 时读回的是"设定值"，下次恢复时再经 setter 补偿，闭环一致，网页端全程无感知。

### 2.2 注入方式（`gui/main_window.py`）

现状 `runJavaScript` 在页面加载后执行，时机太晚（播放器可能已缓存原生 setter 引用），必须改用 `QWebEngineScript`：

- 注册位置：`__init__` 中 `page` 创建之后、`setUrl`（`gui/main_window.py` 加载地址处）之前，通过 `page.scripts().insert(script)`；
- `InjectionPoint = QWebEngineScript.InjectionPoint.DocumentCreation`（页面任何脚本运行之前）；
- `WorldId = QWebEngineScript.ScriptWorldId.MainWorld`（与页面共享原型链；`ApplicationWorld` 是隔离世界，拦截不到页面写入）；
- `RunsOnSubFrames` 按需：播放器音频在主框架，可设 `False`，若验证发现播放器嵌在 iframe 再改为 `True`。

### 2.3 配置项（`profile_manager.py`）

新增配置（跟随现有配置治理方式）：

| 键 | 默认值 | 说明 |
|---|---|---|
| `volume_compensation.enabled` | `true` | 关闭时不注册脚本，行为与现状完全一致（回退路径） |
| `volume_compensation.k` | `0.5` | 补偿指数，范围约束 0.2~1.0；1.0 等效于不补偿 |

### 2.4 设置界面（`gui/settings_dialog.py`，后置）

第一阶段 k 值通过配置文件调整即可；设置界面暴露滑块（0.2~1.0，实时热更新 `window.__NC_VOLUME_K`）作为独立小任务放在第三阶段，不阻塞主链路验证。

---

## 3. 实施步骤

### 第一阶段：核心注入与验证

- [x] 新建 `gui/volume_compensation.js`（setter 补偿 + getter 逆变换 + `window.__NC_VOLUME_K` 热更新）
- [x] `gui/main_window.py`：`__init__` 中 `setUrl` 之前注册 `QWebEngineScript`（`DocumentCreation` + `MainWorld`）
- [x] `profile_manager.py`：新增 `volume_compensation.enabled` / `volume_compensation.k` 两个配置项，注入时读取（实施时另增 `volume_compensation.scale`，见文末总结）
- [x] 功能验证：
  - JS 侧：offscreen 环境实测确认 setter 已被替换、写入 0.25 读回精确等于 0.25（闭环一致）、`window.__NC_VOLUME_K` 热更新生效
  - 听感侧：用户确认真机线性化已生效，低段可闻
  - 持久化侧：getter 逆变换保证读回"设定值"，localStorage 闭环一致
- [x] 日志：注入注册成功/失败 + 劫持安装状态检查均写入现有日志体系（`logger/`）

### 第二阶段：k 值调优

- [ ] 实测 k ∈ {0.3, 0.4, 0.5, 0.6} 的听感，重点验证 1~20 低段区间
- [ ] 收敛默认值并更新 `profile_manager.py` 默认配置
- [ ] 若 k=0.5 附近仍不理想（说明网页内部曲线非纯幂函数），评估换用分段补偿或查表映射，仍复用同一劫持通道

### 第三阶段：设置界面暴露（可选，后置）

- [ ] `gui/settings_dialog.py` 增加"音量线性化"开关与 k 值滑块
- [ ] Qt 侧变更时 `runJavaScript` 热更新 `window.__NC_VOLUME_K`，无需刷新页面

### 第四阶段：文档收尾

- [ ] 更新 `AGENTS.md`（若新增模块/约定）
- [ ] 实施记录归档至 `文档/修改记录/`

---

## 4. 检查点与依赖

| 检查点 | 内容 | 通过标准 |
|---|---|---|
| CP1（第一阶段末） | 劫持生效 | JS 验证 setter 被替换，听感低段改善 ✅ 2026-08-17 通过 |
| CP2（第二阶段末） | k 值收敛 | 选定默认值，1~20 区间调节可用 |
| CP3（全部完成） | 可回退 | `enabled=false` 时行为与现状一致 |

依赖关系：第二阶段依赖第一阶段完成；第三阶段独立于第二阶段，可并行或推迟；无外部依赖。

---

## 5. 风险与回滚

| 风险 | 影响 | 缓解 |
|---|---|---|
| 播放器缓存了原生 setter（劫持失效） | 功能不生效 | `DocumentCreation` 注入保证先于页面脚本；CP1 验证兜底 |
| 网页前端升级为非 `<audio>` 播放（MSE/Web Audio） | 功能静默失效 | 日志记录劫持安装状态；失效时听感回归现状，无破坏性 |
| 播放器对音量做 clamp/校验导致逆变换失真 | 音量条显示异常 | getter 钳制到 [0,1]；听感验证时核对 UI 显示 |
| k 值无全局最优（不同歌曲响度差异） | 调优收敛困难 | 以低段可用性为准，接受中高段轻微偏差 |
| iframe 内播放导致脚本未注入 | 功能不生效 | 验证 `RunsOnSubFrames` 需求，必要时改 `True` |

回滚方式：`volume_compensation.enabled=false` 或直接还原 `gui/main_window.py` 中的脚本注册代码。改动集中在 4 个文件，无配置破坏性变更，新增配置键有默认值兜底。

---

## 6. 实施总结（2026-08-17）

### 6.1 完成情况

第一阶段已实施并通过 CP1（真机听感确认线性化生效）。改动文件：

| 文件 | 改动 |
|---|---|
| `gui/volume_compensation.js` | 新建。劫持 `HTMLMediaElement.prototype.volume`：setter 写入 `m·v^k`，getter 逆变换 `(real/m)^(1/k)` 并钳制 [0,1]；`__NC_VOLUME_K` / `__NC_VOLUME_M` 支持运行时热更新；安装后置 `__NC_VOLUME_COMPENSATION_INSTALLED` 标记 |
| `gui/main_window.py` | `_register_volume_compensation_script()`：`setUrl` 前注册 `QWebEngineScript`（`DocumentCreation` + `MainWorld` + `RunsOnSubFrames=False`）；`_check_volume_compensation_status()`：页面加载后检查劫持标记并写日志 |
| `profile_manager.py` | 新增 `volume_compensation_config.json` 配置（沿用 PipeWire 的 JSON 配置模式）：`enabled`（默认 true）、`k`（默认 0.5，钳制 0.2~1.0）、`scale`（默认 0.3，钳制 0.01~1.0） |
| `packaging/NetEaseMusic.spec` | `datas` 增加 `gui/volume_compensation.js`，保证打包后运行期可按路径读取 |

### 6.2 关键问题与结论

1. **`runJavaScript` 返回 JS 对象序列化不可靠（Qt 6.10.1）**：状态检查最初直接返回对象字面量 `{installed, k}`，Python 侧收到空字符串 `''`，误报「劫持未生效」。实测 `{a:1}` → `''`，而 `JSON.stringify(...)` → 正常字符串。**约定：一切 `runJavaScript` 结构化返回值必须先 `JSON.stringify`，Python 侧 `json.loads` 解析。**

2. **新增缩放系数 m（计划外，听感验证后追加）**：纯幂补偿 `v^k` 抬高低段后，滑块 1 档已有可闻响度，0~1 之间无下调余量。应需求将补偿曲线改为 `h(v) = m·v^k`，把旧曲线 1 档响度锚定到 30 档，使 1~29 档有下调空间。初版按「播放器线性写入 g(s)=s/100」假设取 m=(1/30)^0.5≈0.183；真机试听最大音量不足，最终收敛 **m=0.3**。当前默认参数 **k=0.5, m=0.3**，滑块 100 档真实振幅约 0.3。

3. **验证方法**：无需真机即可验证劫持链路——`QT_QPA_PLATFORM=offscreen` 起 `QWebEngineView` 加载目标页面，`new Audio()` 写入设定值后读回，可验证 setter 替换、闭环一致性（写入 v 读回精确等于 v）与 k/m 热更新。注意 getter 已逆变换，测真实振幅需先临时将 k 置 1.0 再读（此时 getter 返回 `real/m`，需乘回 m）。

### 6.3 当前生效配置

```json
{"enabled": true, "k": 0.5, "scale": 0.3}
```

调参方式：编辑登录数据目录下 `volume_compensation_config.json`（`~/.local/share/netease-music/login_data/`），重启应用生效；回退方式：`enabled=false` 或还原 `gui/main_window.py` 的注册代码。

### 6.4 遗留事项

- 第二阶段（k 值细调）：当前 k=0.5 听感已可用，如需更细可实测 k ∈ {0.3, 0.4, 0.5, 0.6}。
- 第三阶段（设置界面暴露开关与滑块）：未实施，暂以配置文件调参。
- 第四阶段（文档收尾）：实施记录待归档至 `文档/修改记录/`。

---

*计划制定：2026-08-11 19:19 (UTC+8)*
*第一阶段实施与总结：2026-08-17 (UTC+8)*
